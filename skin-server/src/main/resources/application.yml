spring:
  application:
    name: skin-server
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}

logging:
  file:
    name: target/skin.log
  level:
    org.springframework.ai.chat.client.advisor: debug

mybatis:
  configuration:
    map-underscore-to-camel-case: true

server:
  port: 8081
  address: 0.0.0.0
